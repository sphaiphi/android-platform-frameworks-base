# ResourceCertificateSource - Reverse Engineering Documentation

## Executive Summary
`CertificateSource` that reads certificates from an Android raw resource (e.g., `res/raw/my_certs`).

## Java-to-C++ Translation Guide
*   Requires access to Resources (AssetManager).
