# KeymasterIntArgument - Reverse Engineering Documentation

## Executive Summary
Represents a Keymaster argument with an integer value (types `KM_ENUM`, `KM_UINT`, `KM_UINT_REP`, `KM_ENUM_REP`).

## Data Model
*   `value` (int).

## Java-to-C++ Translation Guide
*   See `KeymasterArgument`.
