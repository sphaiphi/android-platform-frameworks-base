# KeymasterDateArgument - Reverse Engineering Documentation

## Executive Summary
Represents a Keymaster argument with a date value (type `KM_DATE`).

## Data Model
*   `date` (Date).
*   Serialized as `long` (milliseconds).

## Java-to-C++ Translation Guide
*   See `KeymasterArgument`.
