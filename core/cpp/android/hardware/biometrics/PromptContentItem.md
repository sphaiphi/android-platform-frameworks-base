# PromptContentItem - Reverse Engineering Documentation

## Executive Summary
`PromptContentItem` is a marker interface for items that can be displayed in the content view of a `BiometricPrompt`.

## Architecture Overview
Base interface for the sealed class hierarchy of content items.

## Java-to-C++ Translation Guide
- **Type**: Abstract base class or `std::variant` of supported item types.
