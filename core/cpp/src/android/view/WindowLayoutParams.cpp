#include <android/view/WindowLayoutParams.h>

namespace android::view {

// WindowLayoutParams uses default constructor; all fields have in-class defaults.
// Constructor delegates to LayoutParams(w, h).

} // namespace android::view
