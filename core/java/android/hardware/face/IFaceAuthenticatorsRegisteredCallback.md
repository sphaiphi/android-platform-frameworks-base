# IFaceAuthenticatorsRegisteredCallback - Reverse Engineering Documentation

## Executive Summary
`IFaceAuthenticatorsRegisteredCallback` is an AIDL callback interface used to notify `FaceManager` when `FaceService` has finished registering all face authenticators (HALs).

## API Reference
- `void onAllAuthenticatorsRegistered(in List<FaceSensorPropertiesInternal> sensors)`: Oneway method.

## Java-to-C++ Translation Guide
- **AIDL**: This is a pure AIDL definition. The C++ code will be generated by the AIDL compiler (`BnFaceAuthenticatorsRegisteredCallback` / `BpFaceAuthenticatorsRegisteredCallback`).

