# KeymasterBlobArgument - Reverse Engineering Documentation

## Executive Summary
Represents a Keymaster argument with a blob (byte array) value (types `KM_BYTES`, `KM_BIGNUM`).

## Data Model
*   `blob` (byte[]).

## Java-to-C++ Translation Guide
*   See `KeymasterArgument`.
