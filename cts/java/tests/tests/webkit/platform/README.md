# Tests which depend on platform APIs

This directory contains the source code for tests which depend on calling
platform APIs not part of the test SDK, such as system APIs or removed APIs.

If you're adding new tests, please prefer to add them to tests/tests/webkit/src
instead.
